## 1. 공통 설정

- [ ] 1.1 `tailwind.config.js`에 `darkMode: 'class'` 추가
- [ ] 1.2 `frontend/js/darkmode.js` 생성 — 토글 함수, localStorage 저장/복원, 아이콘 업데이트
- [ ] 1.3 깜빡임 방지 인라인 스크립트 스니펫 준비 (`<head>` 최상단에 들어갈 코드)

## 2. 로그인 화면 (index.html)

- [ ] 2.1 `<head>`에 `darkMode: 'class'` tailwind 인라인 설정 추가
- [ ] 2.2 깜빡임 방지 인라인 스크립트 추가
- [ ] 2.3 우상단 고정 토글 버튼 추가 (`fixed top-4 right-4`)
- [ ] 2.4 `<body>`, 카드, 입력창, 버튼, 텍스트에 `dark:` 클래스 추가
- [ ] 2.5 `darkmode.js` 로드 및 초기화

## 3. 회원가입 화면 (signup.html)

- [ ] 3.1 `<head>`에 `darkMode: 'class'` tailwind 인라인 설정 추가
- [ ] 3.2 깜빡임 방지 인라인 스크립트 추가
- [ ] 3.3 우상단 고정 토글 버튼 추가
- [ ] 3.4 `<body>`, 카드, 입력창, 버튼, 텍스트에 `dark:` 클래스 추가
- [ ] 3.5 `darkmode.js` 로드 및 초기화

## 4. 팀 선택 화면 (team-select.html)

- [ ] 4.1 `<head>`에 `darkMode: 'class'` tailwind 인라인 설정 추가
- [ ] 4.2 깜빡임 방지 인라인 스크립트 추가
- [ ] 4.3 헤더 우측에 토글 버튼 추가
- [ ] 4.4 배경, 카드, 입력창, 버튼, 텍스트에 `dark:` 클래스 추가
- [ ] 4.5 `darkmode.js` 로드 및 초기화

## 5. 칸반 화면 (kanban.html)

- [ ] 5.1 `<head>`에 `darkMode: 'class'` tailwind 인라인 설정 추가
- [ ] 5.2 깜빡임 방지 인라인 스크립트 추가
- [ ] 5.3 헤더 우측 (user 이메일 왼쪽)에 토글 버튼 추가
- [ ] 5.4 헤더, 사이드바, 칸반 컬럼, 카드, 모달에 `dark:` 클래스 추가
- [ ] 5.5 `darkmode.js` 로드 및 초기화

## 6. 채팅 화면 (chat.html)

- [ ] 6.1 `<head>`에 `darkMode: 'class'` tailwind 인라인 설정 추가
- [ ] 6.2 깜빡임 방지 인라인 스크립트 추가
- [ ] 6.3 헤더 우측에 토글 버튼 추가
- [ ] 6.4 헤더, 메시지 버블, 입력창에 `dark:` 클래스 추가
- [ ] 6.5 `darkmode.js` 로드 및 초기화
