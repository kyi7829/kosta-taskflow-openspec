## 1. 공통 설정

- [x] 1.1 `tailwind.config.js`에 `darkMode: 'class'` 추가
- [x] 1.2 `frontend/js/darkmode.js` 생성 — 토글 함수, localStorage 저장/복원, 아이콘 업데이트
- [x] 1.3 깜빡임 방지 인라인 스크립트 스니펫 준비 (`<head>` 최상단에 들어갈 코드)

## 2. 로그인 화면 (index.html)

- [x] 2.1 `<head>`에 `darkMode: 'class'` tailwind 인라인 설정 추가
- [x] 2.2 깜빡임 방지 인라인 스크립트 추가
- [x] 2.3 우상단 고정 토글 버튼 추가 (`fixed top-4 right-4`)
- [x] 2.4 `<body>`, 카드, 입력창, 버튼, 텍스트에 `dark:` 클래스 추가
- [x] 2.5 `darkmode.js` 로드 및 초기화

## 3. 회원가입 화면 (signup.html)

- [x] 3.1 `<head>`에 `darkMode: 'class'` tailwind 인라인 설정 추가
- [x] 3.2 깜빡임 방지 인라인 스크립트 추가
- [x] 3.3 우상단 고정 토글 버튼 추가
- [x] 3.4 `<body>`, 카드, 입력창, 버튼, 텍스트에 `dark:` 클래스 추가
- [x] 3.5 `darkmode.js` 로드 및 초기화

## 4. 팀 선택 화면 (team-select.html)

- [x] 4.1 `<head>`에 `darkMode: 'class'` tailwind 인라인 설정 추가
- [x] 4.2 깜빡임 방지 인라인 스크립트 추가
- [x] 4.3 헤더 우측에 토글 버튼 추가
- [x] 4.4 배경, 카드, 입력창, 버튼, 텍스트에 `dark:` 클래스 추가
- [x] 4.5 `darkmode.js` 로드 및 초기화

## 5. 칸반 화면 (kanban.html)

- [x] 5.1 `<head>`에 `darkMode: 'class'` tailwind 인라인 설정 추가
- [x] 5.2 깜빡임 방지 인라인 스크립트 추가
- [x] 5.3 헤더 우측 (user 이메일 왼쪽)에 토글 버튼 추가
- [x] 5.4 헤더, 사이드바, 칸반 컬럼, 카드, 모달에 `dark:` 클래스 추가
- [x] 5.5 `darkmode.js` 로드 및 초기화

## 6. 채팅 화면 (chat.html)

- [x] 6.1 `<head>`에 `darkMode: 'class'` tailwind 인라인 설정 추가
- [x] 6.2 깜빡임 방지 인라인 스크립트 추가
- [x] 6.3 헤더 우측에 토글 버튼 추가
- [x] 6.4 헤더, 메시지 버블, 입력창에 `dark:` 클래스 추가
- [x] 6.5 `darkmode.js` 로드 및 초기화
